<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/homeFragment">

    <fragment
        android:id="@+id/homeFragment"
        android:name="br.com.iesb.comprarei.view.fragments.HomeFragment"
        android:label="fragment_home"
        tools:layout="@layout/fragment_home" >
        <action
            android:id="@+id/action_homeFragment_to_productsFragment"
            app:destination="@id/productsFragment" />
    </fragment>
    <fragment
        android:id="@+id/productsFragment"
        android:name="br.com.iesb.comprarei.view.fragments.ProductsFragment"
        android:label="fragment_products"
        tools:layout="@layout/fragment_products" >
        <argument
            android:name="cartName"
            app:argType="string" />
        <argument
            android:name="cartId"
            app:argType="string" />
    </fragment>
    <fragment
        android:id="@+id/newProductFragment"
        android:name="br.com.iesb.comprarei.view.fragments.NewProductFragment"
        android:label="fragment_new_product"
        tools:layout="@layout/bottom_sheet_new_product" >
        <argument
            android:name="cartId"
            app:argType="string" />
        <argument
            android:name="productId"
            app:argType="string" />
    </fragment>
</navigation>